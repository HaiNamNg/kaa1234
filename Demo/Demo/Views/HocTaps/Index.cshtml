@model IEnumerable<Demo.Models.HocTap>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Lop.TenLop)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaSinhVien)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Lop.TenLop)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaSinhVien)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HoVaTen }) |
            @Html.ActionLink("Details", "Details", new { id=item.HoVaTen }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HoVaTen })
        </td>
    </tr>
}

</table>
